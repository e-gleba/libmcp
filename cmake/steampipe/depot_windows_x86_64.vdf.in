// Depot script — Windows x86_64. GENERATED by CMake from
// cmake/steampipe/depot_windows_x86_64.vdf.in; edit the template instead.
// Stage the CPack ZIP contents (CI artifact steam-depot-windows_*_x86_64
// or a local *_release_package ZIP) into the contentroot before upload.
"DepotBuild"
{
    "DepotID"       "@CT_STEAM_DEPOT_ID_WINDOWS@"
    // Overrides the app script's contentroot for this depot.
    "contentroot"   "content/windows_x86_64/"

    "FileMapping"
    {
        "LocalPath"     "*"
        "DepotPath"     "."
        "recursive"     "1"
    }

    // Keep symbols local — ship lean depots.
    "FileExclusion" "*.pdb"
}
