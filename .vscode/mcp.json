{
    "servers": {
        "libmcp-project": {
            "type": "stdio",
            "command": "uv",
            "args": [
                "run",
                "--project",
                "${workspaceFolder}/scripts",
                "libmcp-project-mcp"
            ]
        }
    }
}
